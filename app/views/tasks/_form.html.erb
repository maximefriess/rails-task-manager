<%= form_for(@task) do |t| %>
  <div class="form-group">
    <%= t.label :title %>
    <%= t.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= t.label :details %>
    <%= t.text_field :details, class: 'form-control' %>
  </div>

  <% if !@task.new_record? %>
    <div class="form-group">
    <%= t.label :completed %>
    <%= t.check_box :completed, class: 'form-control' %>
    </div>
  <% else %>

  <% end %>

  <%= t.submit class: 'btn btn-primary' %>
<% end %>
